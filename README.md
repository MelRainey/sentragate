# sentragate
Zero Trust gateway for AI workloads. Every LLM request is identity-verified against Microsoft Entra ID, evaluated against Conditional Access-style policy, scanned for PII/secrets/prompt injection, and audit-logged, before it ever reaches the model.
